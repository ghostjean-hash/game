# Rush Hour 개선 백로그

우선순위 목록. 완료 시 줄 위에 `[done]` 표시 + PROGRESS.md에 기록.

## 1. [done] 상점 (골드 사용처) - 2026-07-01 완성
골드로 캐릭터 색 스킨 → 액세서리(리본/꽃/왕관/나비넥타이) → 보드 테마 순으로 해금·장착. 처리: 스킨/테마/장식 3섹션 모달 + 구매/장착 로직은 기구현이었으나, 주인공이 단일 PNG로 전환되며 스킨 색·머리 장식·표정 반영 함수(`render.setTargetColor`/`setTargetAccessory`/`updateTargetFace`)가 빈 함수(죽은 입력)로 남아 있던 것을 실구현으로 복구. 스킨 색=CSS filter(색조 회전), 장식=이모지 오버레이, 표정=몸통 애니. browser-shot 실화면 검증(스킨 색 변화·왕관 부착·테마 반영 확인).

## 2. [done] 난이도 밸런스 재설계 (심각, 사용자 지적 2026-06-29 → 같은 날 처리)
처리: 난이도를 최소 수 + 얽힘(최적해 distinct 차)으로 재측정. 멀티소스 BFS(풀린 상태 전부에서)로 정확한 최소 수 산출. 후반 "막는 차 3대+ & distinct 5대+" 강제로 1차원 자명 배제. 곡선 입문 1~21 / 쉬움 22~81 / 보통 82~151 / 도전 152~181(최대 16수, 총 181개). 6x6에서 17수+는 희귀해 도전 30개. (초기 단일소스 역BFS는 opt 오측정 버그 → 멀티소스로 수정.) 아래는 원래 기록.


**문제**: 최소 수(opt)만으로 난이도 곡선을 만들어, 60스테이지쯤에도 "차 하나만 치우면 토끼가 빠져나가는" 1차원 단순 퍼즐이 나온다. opt가 낮으면 구조가 자명하다.
**원인**: 생성기가 opt 1~8만 보고 분류. opt가 같아도 "막는 차 1대 비키기"와 "여러 대를 순서대로 밀어야 하는" 구조 복잡도를 구분하지 않음. 곡선도 너무 완만(후반도 opt 3~5대).
**개선 방향**:
- opt 상한을 8 → 더 높게(후반 15~25수). 후반으로 갈수록 가파르게.
- opt 외 "구조 복잡도" 지표 추가: 막는 차 대수, 다단계 의존(A를 비키려면 B를 먼저), 분기 수. 같은 opt라도 복잡한 것을 후반에 배치.
- 곡선 재설계: 초반(~50) 쉬움 유지(성취감), 중반(50~120) opt 6~12, 후반(120~200) opt 13~25 도전.
- 초4 대상이라 후반도 "불가능"이 아니라 "생각하면 풀리는" 수준 유지.

## 3. [done] 진행 맵 - 2026-07-01 검증
186스테이지를 난이도별 별 현황과 함께 한눈에(어디까지 왔나). 성취 가시화. 상단 🗺 버튼 → 모달(`renderMap`). 클리어 수/모은 별 요약 + 난이도별 칸(별 개수·현재 위치 강조·클릭 이동). browser-shot 실화면 검증 완료.

## 4. [done] 힌트 버튼 - 2026-07-01 검증
막히면 골드로 "다음 한 수" 보기(`solveStep` BFS 활용). 골드 부족 시 흔들림+거부음. browser-shot 검증(골드 5 차감 + 추천 차 골드빛 강조).

## 5. [done] 연속 클리어 보너스 - 2026-07-01 검증
쉬지 않고 시간 내 연속 클리어 시 콤보 골드 추가(2연속부터 `min(combo,COMBO_MAX)×STEP`). 시간 초과 클리어면 콤보 0. browser-shot 검증(🔥 2연속 팝업 표시 + 콤보 보너스 골드 반영).

## 6. [done] 사운드 - 2026-07-01 완성
이동 효과음/클리어 팡파레는 기구현(Web Audio 합성, `audio/sound.js`). 이번에 별 효과음(`star`)을 추가해 결과 팝업에서 별 개수만큼 계단식으로 반짝 소리를 낸다. 클리어 재현 시 재생 경로 무에러 확인.
