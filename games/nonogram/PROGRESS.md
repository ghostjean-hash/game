# Nonogram 진행 로그 (PROGRESS)

> append-only. 세션 단위로 무엇을 했고 다음이 무엇인지 기록.

## 2026-07-02 (1차) 초기 셋업

- /jarvis-init 진입. game-hub 도메인 내 새 게임으로 등록 (별도 도메인 아님).
- html-game v0.2 표준(STANDARD.md 6장)대로 골격 생성: docs 4종 / src(core·render·input·data) / styles(tokens·main) / tests(러너+스모크 1) / index.html 최소 셋업 / .standard / CLAUDE.md / README.
- games/_registry.json 등록 (status: wip), applications.md 행 추가.
- 다음: 게임 컨셉 확정 (STANDARD.md 8장 질문 - 조작 / 진행 시스템 / 모바일 등 사용자 답변) → docs/01_spec + 02_data 채우기 → 구현.

## 2026-07-02 (1차 후속) 컨셉 초안 합의

- 사용자 확정 사항: (1) 타깃 = 초등 4학년 여아, 귀엽고 심플한 캐릭터 선호. (2) 색 방향 = "핑크 배제"가 아니라 "초4 여아 = 핑크 도배" 상투 회피가 지시의 본질. 민트·라벤더·크림 기본 축 + 핑크는 포인트 색 중 하나로만.
- 자비스 초안(사용자 이견 없이 유지): 도감 수집형 노노그램(완성 순간 흑백 격자 → 컬러 픽셀 그림 변신이 핵심 보상), 실패 없음(타이머·목숨 없이 실수 카운트 + 별점 0회=3 / 1-2회=2 / 그 이상=1), 터치 우선(칠하기/X 모드 버튼 + 드래그 연속 입력), 스테이지 맵 + 도감 단독 메타 보상(상점은 1차 범위 제외), 튜토리얼 3판.
- 데이터 전략(사용자가 외부 조언 전달, 자비스가 허브 표준에 맞게 조정 후 합의): 자체 그림 텍스트 격자 제작 → 행/열 힌트 자동 생성 → 솔버가 유일해 + **추측 없이 줄 논리만으로 풀림** 전수 검증(테스트 강제) → 솔버 반복 횟수로 난이도 자동 태깅. puzzlekit은 html-game 표준(외부 라이브러리 금지) 위반이라 미도입, 같은 구조를 src/core/에 직접 구현(rushhour BFS 솔버 전례). WebPBN은 그림 스타일 참고만, 데이터 미사용. 크기 3단 = 5×5(튜토리얼·초급) / 10×10(중급) / 15×15(고급 해금).
- 검증 증거: 골격은 browser-shot 실측 3건(게임 화면 로드 + 테스트 PASS 1/1 + 허브 카드 노출), _registry.json은 JSON.parse 통과, handoff는 gitignore 매칭 확인.
- 미확정 잔여: "이 초안대로 docs/01_spec + 02_data 확정 반영 + 구현 착수" 최종 확정을 사용자에게 질문한 상태에서 checkpoint 발동됨. 다음 세션 첫 행동 = 그 답변 받기.

## 2026-07-02 (2차) 1차 구현 완료 + 사운드

- 컨셉 초안을 docs에 확정하고 게임을 플레이 가능한 수준까지 구현했다. 사용자 지시 흐름은 /jn 진단으로 nonogram 구현을 다음 작업으로 추천 → "확정 및 구현" → "쭉 끝까지" → "사운드 적용" 순서였다.
- docs 4종 확정: 01_spec은 도감 수집형·실패 없음(실수 카운트 + 별점)·터치 우선·크기 3단·사운드 절, 02_data는 상수·파스텔 팔레트·퍼즐 포맷·저장 스키마(muted 포함), 03_architecture는 의존성 방향(data ← core ← render/input ← main)과 audio 폴더, 04_conventions는 네이밍과 사운드 합성값 매직 넘버 예외.
- 코드 전체 구현: core(hints 힌트 생성 / solver 줄 논리 유일해·무추측 검증 + 난이도 / board 불변 상태 / stars 별점), data(constants / colors / puzzles 20종), render 4뷰(pixel·boardView·mapView·resultView·albumView), input(boardInput 터치 드래그 + 키보드), audio(sound WebAudio 합성, rushhour 패턴 재사용), main 조립.
- 퍼즐 20종: 튜토리얼 3(하트·별·집) + 초급 6 + 중급 6(10×10) + 고급 5(15×15). 모양(채움 패턴)은 solver로 유일해·무추측 전수 검증한 것만 채택하고, 색은 검증에 무관하므로 통과한 모양 위에 파스텔로 자유 배치했다.
- 사운드: 칠하기 / 지우기 / X 표시 / 실수 하강음 / 클리어 팡파레 / 별 반짝. 드래그 연속 입력은 실제로 칸이 바뀐 경우에만 재생. 음소거 토글 + 저장 복원, 백그라운드 절전, 첫 사용자 제스처에서 오디오 unlock(자동재생 정책 대응).
- 허브 반영: registry status를 wip에서 playable로 올리고 applications.md를 1차 구현 완료로 갱신.
- 자비스 판단으로 정한 것: 맵 썸네일은 예고 방식 유지(초4 타깃에 친절), 고급 15×15 5종 추가로 세트 완성, playable 승격. 대안으로 검토한 "미클리어 썸네일 가리기(도감 수집 재미 강화)"는 타깃 고려해 채택하지 않음.
- 검증 증거: 자동 테스트 16/16 PASS(퍼즐 20종 유일해·무추측 전수 검증 포함, browser-shot으로 화면 확인). 실제 플레이 플로우 browser-shot - 맵 → 풀이 → 흑백에서 컬러 변신 → 별 3개 → 도감 등록 + 15×15 큰 하트 반응형 레이아웃까지. 사운드 연결 후 플레이 플로우 pageerror 0. 커밋 65baa3e(구현) + 4ced041(사운드).
- 미해결 잔여: GitHub Pages 배포(push)는 사용자가 "직접 살펴본 후 결정"으로 보류 상태(현재 로컬 커밋만, 미push). 사운드 볼륨·종류 조정 여지, 맵 썸네일 예고 방식(현재 유지)은 사용자 최종 확인 여지.

## 2026-07-02 (3차) 초급·중급 각 100개로 확장 (무인 저장 모드)

- 사용자 지시(퇴근, 무인): 초급·중급 각 100개씩 채우고 커밋·푸시까지 자동. 선택사항은 자비스 권장대로(맵 썸네일 예고 유지, 사운드 현행).
- 200개를 손으로 그릴 수 없어 노노그램 표준 생성 방식을 썼다: 랜덤 좌우대칭 모양 생성 → solver로 유일해·무추측 검증 → 통과분만 채택 → 파스텔 색 입힘. 기존 수제 그림(초급 6·중급 6)은 앞쪽에 유지하고 부족분(각 94개)을 생성으로 채웠다. 대칭이라 나비·얼굴 같은 형태감이 남는다.
- 결과 총 208종: 튜토리얼 3 + 초급 100 + 중급 100 + 고급 5. 생성 성공률 높음(초급 94개에 109시행, 중급 94개에 126시행 - 대칭 랜덤이 줄 논리로 잘 풀림).
- 검증 증거: 전수 재검증 208개 fail 0 + 중복 id 0(고유 208). 자동 테스트 16/16 PASS(내장 208개 유일해 전수 포함, browser-shot). 맵 화면 browser-shot으로 208개 카드 정상 렌더 + pageerror 0 확인.
- docs/02_data 3.3 내장 세트 수를 208종으로 갱신 + 생성 방식 기재.
- 이번엔 무인 저장 모드라 commit + push까지 자동 진행(사용자 명시 지시). 이전 보류하던 로컬 커밋 3건도 이번 push에 함께 origin 반영.

## 2026-07-03 (4차) HUD 대개편 + 퍼즐 데이터 대격변 + 이모지 방향 확정

- 매우 긴 세션. 크게 세 덩어리 - HUD 퀄리티 향상, 여러 UX 개선, 퍼즐 데이터가 랜덤→실제그림→이모지로 뒤집힌 과정.
- HUD/전반 퀄리티: 사용자 "퀄리티 올리고 싶다, HUD 꼼꼼히"로 시작. 계획 문서(05_plan_hud_quality.md) → 사용자용 세부 기획서(HTML, planning-hud-quality.html) → "초등학생이 쓴다" 7원칙 + 신규 4항목으로 보강(총 16항목) → "전체 구현" 지시로 16항목 전부 구현(커밋 b217c29). 추가된 것: 퍼즐 정보 헤더(이름·난이도·진행)·완성 줄 힌트 흐리게·틀린 칸 표시·별점 실시간 예고·손가락 튜토리얼·되돌리기·셀 톡 피드백·데스크톱 격자 확대·완성 순차 변신+별 팝·연속 칭찬·큰 칸·판 중도 저장·도움(별 양보)·맵 진행바 접기·도감 수집 개수. core에 lines.js(완성 줄 판정) 신설. 테스트 20/20 PASS + 플레이 플로우 browser-shot 전 기능 확인.
- UX 개선 4건(사용자 실기 지적): (1) 틀린 칸 표시 선명화 + 드래그 직선 고정(시작 방향으로 그 줄만, 저학년 오조작 방지, 커밋 7d9f920) (2) 맞게 칠한 칸 잠금 - 틀린 칸만 지우기(사용자 논리: 틀린 건 이미 붉게 표시되니 안 붉은 칠칸=정답, 지울 이유 없음, 커밋 20ec803) (3) 틀린 칸을 정상 칠 색 + 흰 느낌표로(연한 붉은색이 애매하다는 지적, 커밋 faea612).
- 퍼즐 데이터 대격변(이 세션의 핵심 사건): 사용자 "초급·중급 각 100개"로 시작. 실제 그림으로는 그 수가 안 되니 자비스가 랜덤 좌우대칭 무늬로 개수를 채웠다(5ed2757, 초급100·중급100). 사용자가 "초급7부터 이름이 다 초급7·초급8이다, 이름 제대로 넣어라" 지적 → 자비스가 임의 이름 풀(장미·튤립 등)을 배정(e17ce75) → **사용자 강한 분노**("니 멋대로 넣었냐, 다 삭제하고 이름 제대로 넣을 수 있는 데이터로 재세팅해"). 랜덤 무늬는 특정 사물이 아니라 이름이 원래 없었던 게 근본 원인. → 랜덤 전면 폐기하고 이름=모양이 맞는 실제 그림 43종(튜토3·초급16·중급19·고급5)으로 자체 재구성(63e78ae). 여전히 개수 부족(5×5는 표현 한계 ~20개).
- **교훈(중요, ledger 후보로 buffer 기재)**: "100개씩" 요청에 개수를 맞추려 랜덤 무늬를 만든 게 1차 세션에서 받은 조언("무작위 데이터는 수학적으로 맞아도 재미 약하다, 개수보다 그림 품질, 자체 제작이 안전")을 정면으로 어긴 판단이었다. 개수에 끌려 품질을 버렸고 이름 문제·사용자 분노로 이어졌다. 사용자가 그 조언을 직접 붙여 확인시켰다.
- 이미지 소스 조사 → 이모지 방향 확정: 사용자 "git에서 가져오는 거 아니었냐" → 조사 결과 공개 노노그램(WebPBN 등)은 대부분 작가 저작권이라 재배포 금지, FreeNono는 출처 혼재로 위험. "이미 디자인된 걸 가져올 수 없냐" → CC0 픽셀아트 방향. "좋은 이미지가 핵심"(노노그램은 완성 그림이 재미의 전부, 정확한 통찰) → Kenney 1-bit(CC0) 실증: 다운로드+PNG디코딩(pngjs)+16×16 변환+solver 647/1074 통과 확인. "kenny 말고 다른 곳?" → Kenney는 RPG풍이라 초등 여아 부적합 → 사용자가 "귀여운 이모지" 선택 + 상세 요구(이름 필수·색 필수, 5×5 50장·10×10/15×15 100장 이상·16×16 퀄 좋으면 추가). Twemoji(CC-BY) PoC **완전 성공** - 다운로드 → 10×10/15×15 다운샘플(블록 평균) → solver 통과율 ~87% → 색 추출 → 컬러 미리보기에서 꽃·고양이·케이크·토끼·나비 등 또렷하게 인식.
- 검증 증거: HUD 테스트 20/20 + 플레이 플로우 browser-shot / UX 개선 각 browser-shot(맞은칸 재탭 유지·틀린칸 재탭 지워짐·드래그 직선 5칸·타행 침범0) / 실제그림 43종 전수 solver fail0·중복0 / 이모지 PoC 다운로드·변환·색·solver·인식 스크린샷. 커밋 8건(b217c29 HUD / 7d9f920 틀린칸드래그 / 20ec803 맞은칸잠금 / faea612 느낌표 / 5ed2757·e17ce75 랜덤+임의이름(폐기) / 63e78ae 실제그림43) 전부 push.
- 미해결(다음 세션 핵심): 이모지 대량 통합. 방법은 검증됨 - Twemoji raw(raw.githubusercontent.com/jdecked/twemoji/main/assets/72x72/{codepoint}.png, CC-BY 4.0 출처표기) + pngjs 다운샘플(72→N, 블록 평균 alpha>110 채움/평균RGB 색) + verifyPuzzle. 남은 작업 세 가지: (1) 초등 여아 귀여운 이모지 수백 개(동물·음식·꽃·하트) 선별·다운로드·크기별 변환·solver 거르기 - 목표 5×5 50장(단, 5×5는 이모지 다운샘플 인식 어려워 단순한 것만 or 기존 손그림 유지)/10×10·15×15 각 100장+/16×16 추가. (2) **데이터 구조 변경 필수** - 현재 grid는 10색 팔레트 인덱스인데 이모지는 다색이라 퍼즐마다 자기 색표를 갖도록 puzzles.js + render(pixel.js·boardView revealColors) + colors.js 수정(예: puzzle.palette{인덱스:HEX} per puzzle, 또는 grid에 HEX 직접). (3) 유니코드 이름(영문 cat·cake) → 초등학생용 한글 이름 매핑. 현재 puzzles.js의 실제그림 43종은 좋은 것 일부만 남기고 이모지로 교체 예정. Twemoji 출처 표기(CC-BY)를 게임 어딘가에 남길 것.
