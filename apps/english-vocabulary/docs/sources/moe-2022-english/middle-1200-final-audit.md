# 중등 1,200 전수 재검사 기록

사용자 지시 "전수 검사해줘, 빠진 부분 없어?"(2026-07-29)에 따른 독립 재검사. 배치별 `-review.md`의 통과 기록을 근거로 삼지 않고 1,200개를 처음부터 다시 검사했다(글로벌 규칙 - 기존 검수 기록은 검수했다는 주장일 뿐 품질 증명이 아니다).

## 1. 검사 범위와 결과

| 검사 | 방식 | 결과 |
|---|---|---|
| 공식 원천 순번 전수 대조 | 1~1200 순번마다 카드 존재·표제어 일치·ID 일치 | 통과 (누락 0, 불일치 0) |
| 필드 완전성 | 단어·예문·번역·뜻·품사·메타·세트필드·문장부호·예문 길이 | 통과 (결함 0) |
| 전역 중복 | ID·단어·예문·번역·뜻을 1,200개 전체 범위에서 대조 | 통과 (중복 0) |
| 형식 검증기 | `validate-batch.mjs NN --middle` 6배치 | 전 배치 error 0 / warning 0 |
| 사전 2곳 교차 대조 | `lexical-crosscheck.mjs` 실조회 기록 | 품사 불일치 0 (배치 06 `upon`만 한쪽 사전 미수록) |
| 예문 관사·수 | `middle-article-screen.mjs` 후보 전수 사람 판정 | 실제 오류 3건 발견·수정(아래) |
| 뜻과 예문 번역의 일치 | 6배치 후보 전수 사람 판정 | 실제 어긋남 9건 발견·수정(아래) |
| 예문 보조 어휘 난도 | 예문 어휘를 초등 800 + 중등 1,200 풀과 대조 | 목표 단어보다 어려운 것 3건 교체(아래) |
| 앱 유닛 테스트 | `tests/run-node.mjs` | 264 PASS / 0 FAIL |

## 2. 재검사에서 새로 찾아 고친 것 (배치 01·02, 15건)

배치 03~06은 제작 단계에서 이미 같은 관문을 거쳐 이번 재검사에서 추가 결함이 나오지 않았다. 배치 01·02는 이 관문 세 가지(배치 간·내부 내용 중복, 예문 관사·수, 예문 어휘 난도)가 만들어지기 전에 확정된 분량이라 이번에 처음 통과시켰고, 아래를 고쳤다.

### 2.1. 뜻과 예문 번역이 어긋난 것 (9건)

카드 앞면의 뜻과 예문 번역이 서로 다른 말을 쓰면 학습자가 헷갈린다. 둘을 같은 말로 맞췄다.

1. `absolute` - 뜻 "절대적인"인데 번역은 "완전한 침묵". 뜻을 "절대적인, 완전한"으로 넓혀 연결.
2. `accuse` - 뜻 "혐의를 제기하다" / 번역 "비난했다". 번역을 "거짓말했다며 혐의를 제기했다"로 통일.
3. `appropriate` - 번역 "알맞은 옷" → "적절한 옷".
4. `assign` - 뜻 "맡기다" / 번역 "내주셨다". 뜻을 "맡기다, 내주다"로.
5. `bare` - 뜻 "맨몸의" / 번역 "맨발". 뜻을 "맨-, 드러난"으로.
6. `benefit` - 번역 "이점" → "이익".
7. `bump` - 뜻 "부딪힘"인데 예문은 `The car has a small bump.`(차의 흠집). 사실도 틀렸다(bump는 튀어나온 혹). 뜻 "혹" + 예문 `He has a bump on his head.`로 교체.
8. `century` - 번역 "100년 되었다" → "한 세기나 되었다".
9. `chief` - 뜻 "주요한 사람" → "우두머리"(번역과 통일).

추가로 `chip`은 뜻 "조각"인데 예문이 `The cup has a small chip.`(컵의 이 빠진 자국)이라 뜻·번역·예문 셋이 서로 어긋났다. 뜻 "부스러기" + 예문 `Wood chips covered the wet floor.`로 교체했다(배치 04 `piece` "한 조각"과도 구별).

### 2.2. 예문 관사·수 (3건)

`effect` - `had quick effect` → `had a quick effect`. `expense` - `Travel expense rose` → `Travel expenses rose`. (`chip` 예문은 위에서 함께 교체.)

### 2.3. 예문 보조 어휘가 목표 단어보다 어려운 것 (3건)

`captain`(aboard→warmly), `curl`(forehead→face), `extend`(deadline→school holiday).

## 3. 오탐으로 판정해 그대로 둔 것

- 뜻과 번역의 어간이 달라 보이는 후보 240여 건은 대부분 한국어 활용형 차이다(뜻 "얼다" / 번역 "얼 것이다"). 전수 확인 결과 배치 03~06에서 실제 어긋남 0건.
- 관사 누락 의심 후보 48건 중 대부분은 셀 수 없는 명사이거나 관용 표현이다(`in fashion`, `at noon`, `made of metal`, `with force`, `by post`).
- 예문에 쓰인 범위 밖 어휘 220여 종은 대부분 불규칙 과거형(made·felt·wore), 파생어(children·workers·daily), 고유명사(Seoul·Monday)다.

## 4. 재검사 후 상태

카드 1,200개, 오류 0건. `setId`·`learningOrder`는 전부 null(세트 설계 미착수).

---

# 2차 전수 재검사 (같은 날, 사용자 "다시 한번 전수 검사")

1차와 같은 검사를 반복하지 않고 아직 하지 않은 각도로 다시 봤다.

## 5. 새로 한 검사와 결과

| 검사 | 결과 |
|---|---|
| 초등 800 ↔ 중등 1,200 교차 (단어·예문·번역) | 단어 중복 0. **예문·번역 완전 중복 1건 발견·수정** |
| 초등 800 ↔ 중등 1,200 뜻 문자열 중복 | 71건 검출. 동의어 관계 58건은 정상 판정, **한글이 같지만 뜻이 전혀 다른 동음이의 13건 수정** |
| 예문 속 목표 단어의 품사 용법 | 의심 0건 (명사 카드가 예문에서 동사로 쓰이는 식의 어긋남 없음) |
| 뜻 표기 규격 (영문 혼입·문장부호·쉼표 과다·길이) | 이상 0건 |
| 사전 정의 ↔ 한국어 뜻 1,200개 전수 대조 | **실제 어긋남 2건 수정.** 나머지는 사전의 첫 정의가 다른 뜻일 뿐 내 뜻이 유효 |

## 6. 2차에서 고친 것 (16건)

### 6.1. 초등과 예문이 통째로 같던 것 (1건)

`bloom` - 예문 `Flowers bloom in spring.`과 번역이 초등 `spring` 카드와 완전히 같았다. `Roses bloom in early June.`(장미는 6월 초에 핀다)로 교체.

### 6.2. 초등 단어와 한글 뜻이 같아 딴 뜻으로 읽히던 것 (13건)

한글만 보면 다른 개념으로 읽히는 것들이다. 예를 들어 `cage`의 뜻이 "우리"면 초등 `we`("우리")와 글자가 같아 뜻이 통하지 않는다.

`bury` 묻다→땅에 묻다(초등 ask), `cage` 우리→짐승 우리(we), `distance` 거리→거리(간격)(street), `fever` 열→열(체온)(ten), `glue` 풀→접착제(grass), `happen` 일어나다→생기다(wake), `harm` 해→해로움(year), `rat` 쥐→큰 쥐(mouse), `ride` 타다→타고 가다(burn), `sour` 신→신맛의(god), `species` 종→생물 종(bell), `spend` 쓰다→돈을 쓰다(write), `spot` 점→얼룩(point).

`happen`은 "발생하다"로 바꿨다가 중등 `occur`와 겹쳐 "생기다"로 다시 조정했다.

### 6.3. 사전 대조에서 나온 것 (2건)

`junior`·`senior` - 뜻이 "후배"·"선배"이고 예문이 `He is my junior at school.` / `My senior helped me today.`였다. 한국식 영어라 원어민 표현이 아니다. 뜻을 "손아래 사람"·"손위 사람"으로, 예문을 `He is three years my junior.` / `She is two years my senior.`로 교체.

## 7. 오탐으로 판정한 것

- 초등과 뜻 문자열이 같은 나머지 58건은 정상적인 동의어 관계다(`aim`/`goal` 목표, `assist`/`help` 돕다). 학습 층이 달라 문제가 되지 않는다.
- 사전의 첫 정의가 내 뜻과 달라 보이는 다수는 다의어의 정의 순서 문제였다(`lead` 첫 정의 "납으로 덮다" / 내 뜻 "이끌다", `pitch` "역청을 바르다" / "공을 던지다", `post` "기둥" / "우편"). 예문과 뜻이 서로 맞으므로 정상.
- 사전 정의 추출이 고유명사·다른 언어 항목을 집어온 경우가 있다(`few`·`pan`·`neighbor` "A surname", `labor` "호주 노동당", `unless` "소설 제목"). 카드 내용과는 무관.
- `advertize`는 사전에 "비표준 철자"로 표시되지만 교육부 공식 목록의 표제어가 그것이라 원천을 따랐다.

## 8. 2차 재검사 후 상태

중등 내부 오류 0건, 초등 교차 단어·예문·번역 중복 0건. 앱 유닛 테스트 264 PASS / 0 FAIL.
