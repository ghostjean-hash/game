# game-hub NEXT-SESSION

> 다음 세션 진입 시 우선 읽기. SessionStart hook 자동 주입 대상 (§8.3).

## 직전 작업 (2026-08-06, 아이폰 미니 손 조작 - 노노그램 확대·이동 + 러시아워 화면 정리)

실제 기기에서 손가락으로 안 되던 두 게임을 이어서 처리했다. 배포까지 끝났다(캐시 번호 v208).

**노노그램** - 큰 판의 칸이 손가락보다 작았다. 재보니 5칸판 40px, 10칸판 20px, 15칸판 18px로 애플 권장 44px의 절반 이하다. 병목은 가로였고(세로는 절반 넘게 빔), 왼쪽 힌트 70px + 격자를 정중앙에 놓으려 오른쪽에 비운 70px이 화면 폭의 37%를 먹고 있었다. 여백을 다 걷어도 15칸판은 20px이 상한이라 한 화면에 다 넣는 방식으로는 답이 없다는 것을 수치로 보이고 세 갈래를 물어, 사용자가 두 손가락 확대·이동을 골랐다. 전체를 넣어 26px 이상이면 그대로, 미만이면 오른쪽 여백부터 버리고, 그래도 모자라면 34px로 키워 확대·이동에 맡긴다(15칸 18→34, 10칸 20→27, 5칸 40 그대로). 오므리기 한 번이 곧 전체 보기라 별도 버튼은 없고, 밀어도 숫자 힌트는 왼쪽·위에 붙어 있다. 한 손가락 칠하기와 부딪히는 지점 둘(둘째 손가락이 칸을 칠하던 것, 확대하려다 첫 손가락이 먼저 닿아 실수로 세던 것)도 실측으로 잡았다.

**러시아워** - 신고 셋(메뉴 버튼 안 보임·왼쪽 치우침·하단 엉망)이 한 화면에 몰려 있었다. 놀이 중 위쪽 띠를 통째로 감춰 나가기·소리·설정이 사라져 있었는데, 감춘 이유("보드를 키우려고")가 실측과 맞지 않았다 - 보드는 가로가 병목이라 세로가 349px 남는다. 팝업용 어두운 글자색이 저녁 배경 위에 그대로 얹혀 아이콘이 배경에 묻히던 것도 함께 고쳤다. 치우침은 보드판만 중앙에 두고 출구를 정렬에서 빼던 옛 사양이 원인이라 묶음으로 중앙에 놓았고(보드 270→296px), 하단은 버튼 다섯을 두 줄로 나눠 엄지 쪽 셋을 44px로 키웠다.

검증 - 노노그램 31/31(확대 계산 11건 신설)·러시아워 21/21·루트 172건 전량 통과, 실제 두 손가락 제스처 흉내로 확대·밀기·축소·칠하기 확인, 러시아워 4종 화면에서 화면 밖 버튼 0. 배포 후 일곱 주소 정상, 콘솔 오류 0.

**다음 행동** = (1) 실기기 확인은 사용자 몫 - 아이폰 미니에서 노노그램 큰 판을 두 손가락으로 확대·이동해 실제로 칠할 수 있는지, 러시아워 상단·하단이 손에 맞는지. (2) 앞 세션에서 넘어온 대기는 그대로 - 테트리스 이어서 하기 미구현, 아이폰 브라우저 창 높이 실측, 글로벌 인계 대기 1건(buffer). (3) 영어 단어장 쪽 남은 건은 그 작업 기록(PROGRESS 55절) 참조 - 이미 외운 단어를 나중에 묻는 통로 미구현.

## 직전 작업 (2026-08-06, 영어 단어장 - 이미 아는 단어를 영원히 묻어두기)

사용자가 단어장을 쓰다 부딪힌 문제다. banana처럼 이미 아는 단어를 "알았음"으로 넘겨도 외운 단어 보관함에 남아 복습 목록에 계속 보인다. 사용자 표현은 "영원히 나오지 않게 묻어 버리고 싶다"였다.

단어 상태가 계속 도는 것과 외웠지만 복습 대상으로 남는 것 둘뿐이어서 banana를 담을 자리가 없었던 것이 원인이다. 세 방식(그 화면에서 바로 / 세트 시작 전 목록에서 일괄 / 외움 처리 후 보관함에서) 중 사용자가 첫 번째를 골랐다.

뜻을 확인한 상태에서만 화면 맨 아래에 작은 버튼 "이 단어 다시 안 보기"가 나온다. 판정 버튼과 크기·위치·색을 다르게 했고 키보드 단축키는 일부러 주지 않았다(오타 한 번에 단어가 사라지는 쪽이 더 나쁘다). 묻은 단어는 학습·복습·"못 외운 단어 모음" 어디에도 안 나온다.

진도 세는 기준도 바꿨다 - 200단어 중 12개를 묻으면 "188개 중 몇 개"로 표시하고, 원본 개수는 홈 화면 "묻은 단어 (원본 200개 중)" 칸에 따로 보여준다. 세트 선택 카드와 층 합계까지 같은 기준이다. 되돌릴 문은 둘 - 기존 "방금 처리 되돌리기", 그리고 홈·완료 화면의 "묻은 단어 (N)" 목록에서 단어별 되살리기(전부 묻어 완료로 넘어간 경우에도 진입 가능).

검증 - 순수 로직 검사 294건 전량 통과(신규 2묶음), 실브라우저 5회로 버튼 노출·200→198·목록·되살리기 199 복구·되돌리기 200 원복·메뉴 카드 표시 확인, 콘솔 오류 0.

**다음 행동** = (1) 이미 외운 단어를 나중에 묻는 통로(외운 단어 목록에서 묻기)는 미구현이고 사용자 결정 대기다. (2) 세트 시작 전 목록에서 여러 개를 한꺼번에 묻는 두 번째 방식도 필요해지면 추가 가능(첫 방식과 겹치지 않음). (3) 게임 쪽 실기기 확인·아이폰 브라우저 창 높이 실측은 종전대로 사용자 대기. (4) 글로벌 인계 대기 1건이 buffer에 남아 있다 - 글로벌 세션에서 병합.

## 직전 작업 (2026-08-01, 공용 프레임 3단계 - 나머지 네 게임 적용 + 구글 로그인 수정)

이 세션은 둘을 했다. 앞은 구글 로그인 신고 처리, 뒤는 공용 프레임 3단계다. 둘 다 배포까지 끝났다.

**구글 로그인** - 팝업이 두 번 뜨고 한 시간마다 로그인이 풀린다는 신고 셋이 한 뿌리였다. 구글 부품에 prompt를 비워 요청하면 창이 안 뜬다고 보고 만든 지난 처방이 틀렸다 - 그 요청도 팝업을 열기 때문에 사용자 조작이 없는 시점(만료 5분 전 예약 갱신)에 브라우저가 차단했다. 검사 코드까지 같은 오해를 옮겨 적어 사고가 있는 채로 통과하고 있었다. 조용한 경로에서 창을 여는 호출을 전부 없애고, 만료된 로그인은 허브 진입 때 같은 창을 구글에 다녀오게 해서 되살린다(주소 이동이라 팝업 차단과 무관). 중복 요청도 하나로 묶었다. 구글 콘솔에 돌아올 주소를 등록해야 작동하는 방식이라, 그 주소로 직접 요청해 어디로 돌아오는지 보는 방법으로 두 번 실측했다(1차 미등록 확인 → 사용자 등록 → 2차 복귀 확인 후 스위치 켬). 검사는 새 전제로 다시 써서 30건 통과.

**공용 프레임 3단계** - 테트리스·비행 슈팅·러시아워·스도쿠에 붙여 다섯 게임이 같은 시작 흐름을 쓴다. 테트리스는 주소 쿼리로 페이지를 다시 읽던 모드 선택이 고르는 칸으로, 비행 슈팅은 전용 메뉴가 규격 자리로 옮겨지고 자체 이동 기록 관리를 걷어냈고, 러시아워는 시작 화면이 새로 생겨 진행 맵이 다음 칸으로·상점이 조작단 맨 아래 줄로, 스도쿠는 시작 화면·소리 버튼·이어서 하기가 새로 생겼다(유일한 흰 바탕이라 밝은 톤). 배경 그림이 없는 셋은 게임 요소로 배경을 그렸다(조각 쌓인 판 / 48번 문제 배치 / 입문 문제 9칸 판).

**규격 빈틈 셋을 새로 찾아 표준 4.8 규칙 16-18로 반영**(v0.4.2) - 켜고 끄는 것은 고르는 것과 다른 자리(친구 동행·자동 플레이) / HUD를 캔버스 위에 겹친 게임도 플레이에서 규격 띠를 안 씀 / 게임이 자기 이동 기록을 쌓고 있으면 먼저 걷어냄. 실측 사고 하나도 있었다 - 프레임이 미리 만든 빈 화면 자리가 게임 화면과 같은 이름으로 남아 높이를 반씩 나눠 가졌다(보드가 절반 크기). 공용에서 앞 자리 이름표를 떼도록 고쳤다.

검증 - 루트 검사 프레임 23→28건 + 전량 통과, 게임 자체 검사(러시아워 21/21·비행 슈팅 146/146·노노그램 20/20), 네 게임 실화면 흐름 캡처 콘솔 오류 0, 배포 후 일곱 주소 확인. 캐시 번호 v207, 기획서 Ⅰ권 r1.7.

**배포 설정 결함 셋도 고쳤다** - 커밋 메시지가 옛 값으로 고정돼 이번 세션 두 커밋에 엉뚱한 제목이 남았고(세 번째 커밋 메시지에 정정 기재), 배포 후 확인이 테트리스에서 지금 없는 요소를 보고 있었고, 프레임 검사와 설계 문서 폴더가 배포 검사·커밋 범위에서 빠져 있었다.

**다음 행동** = (1) 실기기 확인은 사용자 몫 - 폰·태블릿에서 네 게임 시작 화면이 한 화면에 들어오는지, 되돌아가기가 기기 버튼으로도 한 칸씩인지, 구글 로그인 창이 한 개만 뜨고 한 시간 후에도 유지되는지. (2) 테트리스 이어서 하기는 미구현(보드 전체 저장이 필요해 범위 밖) - 필요하면 별도 작업. (3) 아이폰 브라우저 창 높이 실측은 종전대로 대기. (4) 글로벌 인계 대기 1건이 buffer에 남아 있다(문서를 한 줄 셸 명령으로 고치다 파일이 조용히 손상된 사고 + 처방안) - 글로벌 세션에서 병합. (5) PROGRESS.md는 이번 봉합에서 월 단위로 나눴다(6~47절 → archive/PROGRESS-2026-06-07.md).

