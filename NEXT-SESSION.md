# game-hub NEXT-SESSION

> 다음 세션 진입 시 우선 읽기. SessionStart hook 자동 주입 대상 (§8.3).

## 직전 작업 (2026-09-05~06, 게임 공용 UI - 부품은 공용 자리는 게임, 걸음 1~2 일부)

사용자 물음 "공용 UI를 새로 만들까 업그레이드할까"에서 시작해 업그레이드로 갔다. 새로 만들 이유가 없었다 - 시작 흐름 부품은 이미 게임 여섯이 함께 쓰고 있었고, 문제는 플레이 화면 쪽 절반이 비어 게임마다 같은 부품을 다시 만든 것이었다(아이콘 버튼 일곱 벌·덮는 카드 여섯 벌·알림 쪽지 다섯 벌·환경설정 다섯 벌).

**가장 중요한 발견** - 공용 상단 띠가 폐기될 때 그 띠에 기능을 위임했던 게임 둘이 기능을 통째로 잃은 상태였다. 러시아워는 어느 화면에서도 소리를 끌 수 없었고(음소거 호출 0건) 스도쿠는 소리와 되돌아가기를 잃었다. 정리 대상이 아니라 이미 나 있던 고장이라, 잔재 제거를 마지막으로 미루고 대체물부터 세우도록 순서를 바꿨다.

**한 것 셋** - (1) 기준 문서 세 층 정렬(`0b41941`): 표준만 한 세대 뒤에 남아 폐기된 규격을 적고 있었다. 표준 v0.4.4로 규칙 3·4 정정 + 11·17 폐기 + 19·20 신설, 기획서 Ⅰ권 r2.1(11장 신설), 백지였던 0권 본문 작성. (2) 설계(`4e54c3d`). (3) 공용 부품 둘 신설 + 러시아워 적용 - 환경설정 그릇(소리를 공용 기본 항목으로)과 덮는 카드·알림 쪽지, 시작 화면 환경설정 자동 삽입, 러시아워 놀이 중 되돌아가기·소리·환경설정 복구.

**검사를 세 번 돌렸고 매번 실제 결함이 나왔다.** 1회차 - 시작 화면에서 카드를 열고 기기 뒤로가기를 누르면 카드가 닫히는 대신 게임을 나갔다(그 화면은 되돌아갈 자리가 하나뿐이라 뒤로가기가 문서를 떠난다). 카드가 자기 자리를 쌓고 소모하는 구조로 뒤집었다. 2회차 - 닫기 연타로 두 칸이 물러나 플레이에서 시작 화면까지 밀렸다. 3회차 - 그 예약 방식 자체가 덫이었다(예약과 도착 사이에 자리가 더 쌓이면 엉뚱한 카드가 떼어지고 닫으려던 카드는 영영 안 닫힌다). 닫기를 예약으로 두지 않고 **먼저 떼고 자리를 뒤에 소모하는** 구조로 뒤집었다.

**검증** - 루트 검사 178건 전량 통과. 마지막 구조 변경 뒤 실화면을 다시 밟았다(부품 넷 콘솔 오류 0, 게임 여섯 시작 화면 세로 넘침 없음, 러시아워 시작 화면·플레이 연타 닫기·되돌아가기 계단). 다만 새 검사는 소스 문자열 대조라 되돌아가기 동작을 실제로 돌려 보지 않는다 - 화면을 흉내 내는 검사가 다음 걸음 첫 항목이다(설계서 5.9).

**다음 행동** = 설계서 `docs/plans/design-2026-09-05-shared-ui-parts.md`가 단일 기준이다. 남은 것 - 나머지 다섯 게임 적용(스도쿠 기능 복구 포함), 러시아워 꾸미기·상점을 공용 덮는 카드로, 폐기 잔재 제거(5장), 되돌아가기를 실제로 도는 검사로 고정(5.9).

## 직전 작업 (2026-09-04, 게임 이동 구조 개편분 봉합 + AGENTS.md 등록, 개발 작업 없음)

이 창은 코드를 만들지 않았다. 앞 창(codex 작업분)이 커밋하지 않고 남긴 36건을 확인하고 네 덩어리로 나눠 커밋·배포했다.

**커밋한 것 셋** - (1) 게임 이동 구조 개편(`e449883`): 화면 위에 떠 있던 공용 나가기 버튼을 없애고 게임 홈 화면 안의 `<-` 하나로 통일했다. 프레임이 `mountHubBack`과 `frame.navigate.back()`을 주고 게임은 스스로 주소를 바꾸지 않는다. 전체화면 버튼은 상단 띠에서 빠졌고 환경설정은 시작 화면 추가 항목으로 옮겼다. 허브 첫 화면의 인라인 스타일 275줄은 `shared/hub.css`로 분리했다(캐시 v232). (2) AGENTS.md 아홉 개 등록(`5ab5efb`): 게임·앱 폴더와 도메인 루트 `.codex/`에 codex가 읽는 이름으로 컨텍스트를 복제했다. (3) 크롬 실행 바로가기(`29341f3`).

**빈 파일 두 개를 발견했고 하나는 되살렸다.** `standards/html-game/STANDARD.md`가 0바이트였다(직전 커밋 기준 245줄, 20KB). 앞 창이 16시 35분에 내용을 옮기다 끊긴 것으로 보이며, 그 상태로 커밋하면 표준 본체가 저장소에서 사라진다. 마지막 커밋본으로 되살린 뒤 커밋했다. `standards/html-game/plans/doc/platform-navigation.html`도 0바이트인데 이쪽은 새 파일이라 되살릴 원본이 없다. 프레임 코드 주석과 기획서 목차(`plans/nav.js`)가 이미 이 문서를 기준 문서로 가리키고 있어 빈 채로 커밋했다 - 목차에서 누르면 백지가 뜬다.

**검증** - `tests/*.test.mjs` 다섯 파일 145건 전부 통과(0 실패). 화면 실측은 하지 않았다.

**다음 행동** = (1) `platform-navigation.html` 본문 작성 또는 목차 항목 회수 중 하나가 필요하다(사용자 결정 대기). (2) STANDARD.md를 정말 개편하려던 것이었다면 어느 내용을 어디로 옮기려 했는지 앞 창 의도 확인이 필요하다. (3) 영어 독해 지문 수정 일감(3묶음 중단, 병합·검증·배포 미실행)은 변동 없다 - 이 창에서 건드리지 않았다.

## 직전 작업 (2026-08-21, 지뢰찾기 새 게임 - 기획서와 설계까지, 구현 전)

허브 여섯 번째 게임으로 지뢰찾기를 착수했다. 이 세션은 기획서와 설계 문서까지고 판 규칙 코드는 아직 없다. 이 창은 다른 머신 작업 34건이 원격에 앞서 있는 낡은 상태에서 작업해, 봉합 때 원격 최신 위로 다시 얹었다(games/mines는 새 폴더라 충돌 없음, 기록 문서 둘만 원격판을 취해 재작성).

**절차를 한 번 건너뛰고 사용자 지적으로 되돌렸다.** `/jarvis-init 지뢰찾기 게임`으로 진입했는데, 그 명령은 새 도메인 등록용이라 게임 하나에는 맞지 않는다는 판정까지는 정확했다. 그런데 그 뒤 구현 파이프라인으로 갈아타지 않고 개발용 문서 넷과 화면 뼈대 코드를 바로 만들었다. 사용자 지적("기획서 작성은 안해? 워크플로우가 어떻게 돼?")이 정확했고, 사람이 읽는 기획서·설계 확인 자리·작업 목록 등록이 빠져 있었다. 원인은 진입 명령과 만들기 절차 사이 이음매 부재라 원장 후보로 buffer에 올렸다(글로벌 세션에서 병합).

**사용자가 정한 것 셋** - 게임 허브의 새 게임으로(별도 도메인 아님) / 난이도는 모바일 맞춤 3단(쉬움 9×9·10, 보통 12×12·25, 어려움 14×18·55, 원작 3단 미사용) / 깃발은 길게 누르기와 모드 전환 버튼 둘 다.

**자비스가 정하고 기획서에 근거를 남긴 것** - 첫 탭은 반드시 안전(지뢰를 첫 칸 여는 순간 놓고 그 이웃까지 비움, 이것이 목숨을 안 두는 근거) / 빠른 열기는 편의 기능이지 안전 장치가 아님(깃발이 틀리면 패배) / 물음표·되돌리기·사용자 지정 난이도·통계·두 손가락 확대는 뺐고 뺀 이유와 다시 볼 조건을 표로 남김.

**화면 그림 세 장을 띄워 보고 다섯 가지를 고쳤다** - 배경 격자 잘림, 깃발 아이콘 뭉개짐, 열린 칸과 닫힌 칸 구분 안 됨(데이터 문서와 코드 색값까지 함께 고침), 정보 둘이 화면 위아래로 갈려 눈이 왕복함, 결과 카드가 다 찾아낸 판을 가림. 플레이·결과 화면의 판은 손으로 그리지 않고 실제 규칙으로 계산했다(지뢰 25개 첫 탭 안전 배치 + 연쇄 열기로 64칸 열림, 숫자는 전부 계산값).

**다음 행동** = (1) 구현은 사용자 지시로 codex가 맡는다 - 판 규칙과 자동 검사 → 격자 화면과 손가락 조작 → 공용 프레임 연결 → 기록 → 실기 확인 순서. `games/mines/docs/planning-mines.html`이 기준이고 어긋나면 기획서가 진실이다. (2) 지금 있는 화면 뼈대(index.html·CSS·수치 상수)는 기획서 확정에 따라 다시 손볼 대상이다. (3) 등록부와 표준 적용 목록에는 아직 안 올렸다 - 들어갈 화면이 없는 상태로 허브 카드를 띄우지 않기 위함. (4) 작업 목록 등록 여부는 사용자 결정 대기. (5) 영어 독해 쪽 진행 중 일감(지문 수정 3묶음 중단, 병합·검증·배포 미실행)은 그 작업 기록 참조 - 이 창에서 건드리지 않았다.

