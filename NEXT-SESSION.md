# game-hub NEXT-SESSION

> 다음 세션 진입 시 우선 읽기. SessionStart hook 자동 주입 대상 (§8.3).

## 0. 사용자 결정

<!-- user-decisions:start -->
- **방향** · 게임 전부를 고도(Godot) 엔진으로 옮긴다. 지금이 아니라 웹에서 모든 작업이 끝난 뒤다. 그때까지 웹 허브 작업은 순서를 바꾸지 않는다 [T-115 · T-116]
- **순서** · 러시아워 화면은 바깥틀(A)을 먼저 끝내고 보드 안쪽(B)은 별도 판으로 나중에 한다. 사용자가 "본질은 B"라고 했으므로 B를 버린 것이 아니다 [T-116]
- **금지** · 러시아워를 기준 게임으로 완전히 올리기 전에는 다른 게임에 손대지 않는다 [T-115]
- **선호** · 오늘운동 앱 화면은 밝은 바탕으로 간다. 허브의 다른 화면은 어두운 바탕이지만 이 앱만 다르다 [T-117]
<!-- user-decisions:end -->

## 직전 작업 (2026-09-17, 오늘운동 TodayFit - 기획 확정 + 설계 + core 구현)

**`apps/todayfit` 신설. 기획서 확정 → 설계 문서 넷 → `core/` 구현까지 왔고 테스트 146건이 통과한다.** 미커밋 0, 화면은 아직 없다.

**이 앱은 게임이 아니라 `apps/` 도구류라 T-115의 "러시아워 전에 다른 게임 금지"에 걸리지 않는다.** 러시아워 일감(T-115 걸음 D·E, T-116 보드 안쪽 B)은 열린 채 그대로다.

**구조의 뿌리는 5계층이다** - 운동 템플릿 → 루틴 → 요일 규칙 → 날짜별 계획 → 수행 기록. 앞 셋은 참조로 잇고 **뒤 두 자리에서 값을 복사해 참조를 끊는다**(월간 계획 생성 시점, 운동 시작 시점). 과거 기록 불변과 앞날 계획 보호가 이 원리 하나로 풀린다. 루틴 항목이 세트 수·횟수·시간을 덮어쓸 수 있어 같은 운동을 템플릿 둘로 복제하지 않는다(값 사다리 3단).

**가장 넓게 번진 결정은 "운동 구간은 시간으로 끝나지 않는다"다.** 횟수 기준 운동이라 45초에 10회를 못 끝낼 수 있고 앱이 끊으면 방해가 된다. 준비·휴식·전환 셋만 시간으로 자동 이동하고 세트는 사용자가 `다음`을 눌러야 끝난다. 이 결정 때문에 `세트당 수행시간`을 `세트당 권장 수행시간`으로 개명하고 문서 전역을 고쳤다.

**기획서가 설계로 넘긴 여섯을 전부 닫았다** - 저장 방식·화면 꺼짐 방지·길게 누르기·소리 잠금 해제·음성·오프라인. 화면 꺼짐 방지는 이 저장소 첫 사용이라 선례가 없고, 한국어 음성이 없는 기기에서는 비프음만 낸다.

**브라우저 사정을 `src/platform/` 셋에 가뒀다**(화면 꺼짐·음성·시각). 고도로 옮길 때 걷어낼 자리를 미리 한곳에 모은 것이다.

**다음 행동은 화면이다** - `store/repo.js`와 `platform/` 셋 → `index.html`·`styles/`와 화면 여섯 → 등록부 등재 → 폰 크기 검사. 규칙은 화면 없이 전부 검증됐으므로 화면은 그 위에 얹기만 하면 된다.

**공용 자산에 손댈 자리 셋이 남아 있다**(구현 단계에서 닿는다) - `shared/cloud/policy.js`(진행 중 세션을 계정에 올리지 않게 제외), `apps/_registry.json`(등재), `service-worker.js`(배포 시 판 번호).

## 직전 작업 (2026-09-06, 걸음 C 완료·배포 + 러시아워 플레이 화면 정리·배포)

**두 판이 닫혔고 둘 다 배포됐다.** 커밋은 `f1d1055`(걸음 C) → `b79df4f`(화면 정리 설계) → `bc0ca32`(화면 정리). 미커밋 0, origin 반영 완료.

**걸음 C - 지갑·상점·꾸미기를 플랫폼으로.** 공용 부품 셋 신설(`shared/frame/wallet.js` · `shop.js` · `shopcard.js`). 꾸미기는 따로 만들지 않고 **값 없는 상점**으로 뒀다. 러시아워에서 상점·꾸미기 팝업 코드와 마크업·CSS, 골드 직접 조작 다섯 자리가 걷혔고 **걸음 A의 임시 변환기가 완전히 사라졌다**. 저장 형식·판 번호는 그대로라 이미 있는 골드와 산 물건이 제자리에 있다. 독립 검사 2회차까지 돌려 통과(적발 18건 전량 반영). **치명 둘** - 저장 칸은 하나인데 그것을 읽는 부품을 둘 세우면서 각자 캐시를 갖게 뒀다. 꾸미기에서 캐릭터를 한 번 고르면 산 물건이 통째로 미보유가 되고(장착은 남아 화면은 멀쩡해 보인다), 한쪽이 다른 쪽 선택을 옛 값으로 되돌렸다. 캐시를 걷고 손댄 종류 하나만 갈아 끼우는 것으로 닫았다.

**러시아워 플레이 화면 바깥틀 정리(T-116).** 사용자가 "게임 안 UI/UX가 개판"이라 지적했고 범위를 A(바깥틀)로 좁혔다. **가장 큰 것은 고장이었다** - 가로 배치 규칙을 페이지에 걸어 뒀는데 한 달 전 공용 프레임이 들어오며 화면 래퍼가 한 겹 생겨 규칙이 통째로 안 먹고 있었다(보드가 조작 줄을 50px 파고들고 오른쪽 567px이 빔). 골드·이동·시간을 보드 위 카드 한 장으로 모으고, 보드 폭 상한 480→720px, 조작을 자주 누르는 셋과 판 옮기는 둘로 갈랐다. 독립 검사 13건 중 고칠 값 있던 11건 반영. **이 판에서 CSS 특이도 사고를 세 번 냈다**(판 이동 버튼 색·"쓰는 중" 테두리·골드 값 색) - 색을 좁은 자리에 덮어쓸 때 그 자리가 일반 규칙보다 뒤인지 매번 봐야 한다.

**다음 행동 - 사용자 확인 대기가 둘, 남은 일감이 셋이다.**

1. **화면 확인** - `http://127.0.0.1:8123/games/rushhour/`(서버 켜 둠) 또는 배포본. 걸음 C는 상점에서 사기·꾸미기에서 캐릭터 바꾼 뒤 산 물건 남아 있는지, 화면 정리는 네 크기 전부.
2. **사용자 결정 둘** - 가로에서 보드가 화면 중심에서 43px 오른쪽이다(조작 열이 왼쪽에만 있어 오른쪽 짝이 없다. 빈 열을 만들면 정중앙이 되는 대신 자리를 버린다). 폰 세로 위 여백 222px은 보드가 화면 폭에 갇힌 물리적 한계라 없애려면 출구 표현을 바꿔야 하고 그건 보드 안쪽이다.
3. **남은 일감** - 러시아워 **보드 안쪽(B)** 개편(블록 경계·캐릭터 크기·출구 표현, 사용자가 "본질은 B"라고 했다) / 플랫폼 **걸음 D**(러시아워가 자기 폴더에서 다시 만든 팝업·버튼·효과 제거 + 폐기 잔재) / **걸음 E**(마무리 확인, 기획서 Ⅲ권 6.3 완료 조건 여섯).

**고도로 옮길 때 먼저 할 일 하나(이번 세션에서 약속했다)** - 규격을 그대로 베끼지 말고 **브라우저 사정 때문에 생긴 모양을 한 번 걸러낸다.** 되돌아가기 계단이 대표다(브라우저 뒤로가기 기록과 짝을 맞추느라 복잡한데 고도에는 그 기록이 없다). 저장을 글자 뭉치로 갖는 것도 브라우저 제약이다. 옮기기 착수 시점에 그 목록을 뽑는다.

**사용자 확정 방향(ROADMAP 기재)** - **게임 전부를 고도(Godot)로 옮긴다. 지금이 아니라 웹에서 모든 작업이 끝난 뒤.** 그래서 순서를 바꾸지 않고 걸음 D·E와 나머지 게임 올리기까지 웹에서 다 한다. 나머지 게임 여섯을 플랫폼에 올리는 일이 단순 이관이 아니라 **규격 시험**이 된다 - 지금 규격은 러시아워 하나에서 뽑은 것이라 둘째 게임이 오면 흔들릴 자리가 있다(오늘도 "여러 개를 함께 쓰는 상점 종류"를 안 만들고 미뤘다). 넘어가는 것은 코드가 아니라 문서다.

## 직전 작업 (2026-09-06, 게임 공통 플랫폼 - 걸음 A·B 완료·배포)

사용자 지시로 판이 한 층 내려갔다. 앞 판(T-114)은 화면 부품 층까지였는데, "새 게임에서 핵심 플레이만 만들고 싶다, 매번 폴리싱을 반복하고 싶지 않다"는 요구가 나오면서 진행·저장·재화·상점·꾸미기·계정까지 공용이 갖는 범위로 넓혔다. **기획서 Ⅲ권 「게임 공통 플랫폼」**을 새로 세웠다(`standards/html-game/plans/doc/game-platform.html`, 목차 A그룹 등록). 러시아워를 기준 게임으로 완전히 올린 뒤에 다른 게임으로 간다는 것이 사용자 결정이다.

**기획서가 못 박은 원칙 둘** - (1) 경계선을 기능이 아니라 **자리**로 긋는다: 플레이가 벌어지는 네모 안쪽은 게임이 전부 갖고 바깥은 플랫폼이 전부 갖는다. (2) 모든 부품은 선택이고 **부르지 않으면 없는 것**이다(기본값은 항상 없음 쪽). 이 둘이 없으면 부품을 늘릴 때마다 기존 게임 여섯이 흔들린다.

**걸음 A 완료** (`4527e9d`) - 플랫폼 저장 칸 여덟(진행·지갑·산 것·쓰는 것·이어서 하기·소리·환경설정 값·게임 자유) 신설 + 러시아워 저장 이관. 옮기기 전 원본을 백업 칸에 통째로 남긴다. 검사 4회차까지 돌려 값이 조용히 사라지는 경로 다섯을 잡았다. 가장 컸던 것은 **계정 저장이 기기 사이를 오간다는 것을 설계에서 빠뜨린 것**이다 - 옛 빌드 기기가 올린 옛 모양이 내려오면 판 번호는 맞는데 내용만 옛것이 되어 진행이 화면에서 통째로 빈다. 모양까지 보는 장치와, 첫 옮기기·다시 옮기기의 우선순위를 가르는 것으로 풀었다.

**걸음 B 완료** (`0ea076e`) - 공용 진행 부품(`shared/frame/progress.js`)과 공용 진행 맵 화면(`shared/frame/mapscreen.js`). 게임은 판 목록만 넘기고 한 판이 끝나면 `finish(id, result)` 하나로 알린다. 별 넷·잠금 넷을 고르는 방식. 러시아워에서 맵 코드 41줄·갈래 탭 처리·별 계산·기록 갱신·다음 이전 넘기기가 걷혔고, 걸음 A의 임시 변환기도 절반이 사라졌다.

**걸음 B에서 크게 잘못한 것 둘.** (1) 맵 코드를 "이 주석부터 저 주석까지"로 잘랐는데 그 사이에 상점·꾸미기 함수 여섯이 끼어 있어 통째로 지웠다. 호출부만 남아 버튼이 죽었는데 **기계 검사 225건이 전부 통과했다**(검사가 게임 본체를 실행하지 않는다). (2) 그것을 고치다 `git checkout`으로 걸음 B 작업분을 통째로 날렸다. 다른 파일은 살아 있어 그 파일만 다시 만들었고, 검사가 "다시 적용하면서 빠뜨린 것 0건"을 확인해 줬다.

**검사를 걸음 B에만 일곱 번 돌렸다.** 같은 지적("지적된 줄만 고치고 같은 종류는 놔둔다")이 네 번 반복됐고 5회차에 가서야 러시아워 문서 넷을 처음부터 끝까지 코드와 대조했다. 화면 확인도 세로로만 해서 가로에서 진행 맵이 왼쪽에 붙는 것을 5회차가 잡았고, 그것을 고치며 만든 회귀(결과 카드가 덮으면 보드가 옆으로 튐)를 6회차가 잡았다. 7회차가 커밋 가능으로 판정했다.

**다음 행동** = 설계서 두 권이 단일 기준이다 - `docs/plans/design-2026-09-06-platform-store.md`(걸음 A), `docs/plans/design-2026-09-06-platform-progress.md`(걸음 B). **남은 것은 걸음 C~E** - C 지갑·상점·꾸미기를 플랫폼으로(그때 임시 변환기의 나머지 절반이 사라진다), D 껍데기 정리(러시아워가 자기 폴더에서 다시 만든 팝업·버튼·효과 제거 + 폐기 잔재), E 마무리 확인. 기획서 Ⅲ권 6.3의 완료 조건 여섯을 다 채워야 러시아워가 끝난 것이고, 그 전에는 다른 게임에 손대지 않는다.

**사용자 확인 대기** - 걸음 A·B 모두 화면 확인 없이 진행 지시를 받아 커밋했다. 실제 진행 데이터가 새 형식으로 옮겨진 상태이고, 이상하면 백업 칸에서 되돌릴 수 있다.

**목표 밖 발견 셋(손대지 않음)** - 가로에서 플레이 화면이 왼쪽에 붙고 오른쪽 절반이 빈다(한 달 전 화면 래퍼가 들어오며 가로 배치 규칙 전체가 죽었다) / 과일 농장이 열리지 않는다(지뢰찾기에서 없는 값을 가져오려다 죽는다) / 과일 농장·지뢰찾기가 게임 목록에 없고 과일 농장은 계정 저장에도 연결돼 있지 않다.

