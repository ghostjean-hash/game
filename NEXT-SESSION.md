# game-hub NEXT-SESSION

> 다음 세션 진입 시 우선 읽기. SessionStart hook 자동 주입 대상 (§8.3).

## 0. 사용자 결정

<!-- user-decisions:start -->
- **방향** · 게임 전부를 고도(Godot) 엔진으로 옮긴다. 지금이 아니라 웹에서 모든 작업이 끝난 뒤다. 그때까지 웹 허브 작업은 순서를 바꾸지 않는다 [T-115 · T-116]
- **순서** · 러시아워 화면은 바깥틀(A)을 먼저 끝내고 보드 안쪽(B)은 별도 판으로 나중에 한다. 사용자가 "본질은 B"라고 했으므로 B를 버린 것이 아니다 [T-116]
- **금지** · 러시아워를 기준 게임으로 완전히 올리기 전에는 다른 게임에 손대지 않는다 [T-115]
- **선호** · 오늘운동 앱 화면은 밝은 바탕으로 간다. 허브의 다른 화면은 어두운 바탕이지만 이 앱만 다르다 [T-117]
- **결정** · 월간 계획 생성은 지난 날짜를 만들지 않는다. 이번 달은 오늘부터 월말까지(오늘 포함), 다음 달 이후는 그 달 전체, 지난 달은 만들기 조작 자체가 없다 [T-117]
- **순서** · 오늘운동을 폰에서 직접 확인하기 전에는 다음 기능이나 구조 변경으로 넘어가지 않는다 [T-117]
- **선호** · 보고는 짧게. 결론을 앞에 두고, 기다리는 동안 중간 알림을 여러 번 올리지 않는다
<!-- user-decisions:end -->

## 직전 작업 (2026-09-18, 오늘운동 월간 계획 대상 기간 + 허브 대화상자 색)

**월간 계획 생성이 지난 날짜를 만들지 않는다.** 이번 달은 오늘부터 월말까지(오늘 포함), 다음 달 이후는 그 달 전체, 지난 달은 만들기 조작 자체를 내지 않는다. 기존 계획이 있는 날짜 건너뛰기는 기간과 별개의 거름이라 둘을 다 통과한 날짜만 만들어진다. 기획서 9.5 · 사양 3.7.1 · 설계 4.7.6.1을 먼저 고치고 코드를 맞췄다(커밋 `7f199b8`).

**시험 213건 통과, 브라우저로 두 바퀴 밟아 확인했다.** 9월 18일 기준 18~30일 열셋만 생기고 1~17일은 계획 없음으로 남는다. 손으로 고친 22일 예정 시간이 재생성 뒤에도 살아 있고, 이번 달 달성률은 0퍼센트가 아니라 `해당 없음`으로 시작한다.

**허브 대화상자가 검은 바탕에 검은 글자로 뜨던 것을 토큰 층에서 고쳤다**(커밋 `ebb63c0`). 허브는 밝은 화면인데 자기 팔레트를 `--hub-*`라는 별도 이름으로만 두어 공용 부품이 어두운 토큰을 읽고 있었다. 허브 화면에서 공용 토큰 자체를 밝은 값으로 다시 정의했고, 게임 화면의 어두운 대화상자는 그대로다.

**배포까지 끝났다** - 배포 판 v243, 주소는 `https://ghostjean-hash.github.io/game/apps/todayfit/`. 배포본에서 새 판 번호와 새 코드 반영을 확인했다.

**다음 행동은 사용자가 폰에서 직접 보는 것이다.** 볼 목록 - 오늘 화면 · 운동 템플릿 · 루틴 · 요일 규칙 · 월간 계획 생성 · 달력 · 오늘 운동 노출 · 세로 화면 가독성 · 터치 영역. 그 확인이 끝나기 전에는 다음 기능이나 구조 변경으로 넘어가지 않는다.

**그 뒤에 볼 것 둘** - `manage.js`가 1000줄을 넘어 화면 묶음별로 가르는 일(설계 2.2.1), 그리고 오늘운동에는 클라우드 병합 규칙이 없어 두 기기를 같은 시기에 만지면 한쪽이 통째로 덮인다는 것(`shared/cloud/policy.js`의 MERGE_RULES에 이 앱 항목이 없다).

## 직전 작업 (2026-09-18, 오늘운동 TodayFit - 관리 화면 아홉 + 달력)

**앱 안에서 한 바퀴가 돈다.** 운동 템플릿 → 루틴 → 요일 규칙 → 월간 계획 생성 → 오늘 화면에 그 운동 표시까지, 저장소에 값을 심지 않고 화면 조작만으로 전 구간을 밟아 확인했다. 어제까지는 들어가도 `오늘 예정된 운동 없음` 하나뿐이었다. 테스트 213건 통과(대상 기간 규칙과 달 키 시험 열셋을 더해 200건에서 늘었다), 배포 판 v241.

**화면 열셋이 전부 섰다.** 이번 판에서 아홉을 더했다 - 설정 `S-03` · 운동 템플릿 `S-09`/`S-10` · 루틴 `S-07`/`S-08` · 요일 규칙 `S-06` · 달력 `S-02` · 날짜 상세 `S-04` · 날짜별 계획 수정 `S-05` · 기본값과 소리 `S-11`. 설계는 `apps/todayfit/docs/05_manage-screens.md`.

**사용자가 기획 결정 넷을 확정했다** - 빈 날짜에 계획 추가는 두고, 다음 달 계획은 직접 만들고, 이미 만든 달은 다시 만들지 않고, 과거 기록은 손대지 않는다. 셋째에서 자비스 추천이 뒤집혔고 그 근거가 정확했다 - **저장에 자동 생성분과 손으로 고친 계획을 가르는 표식이 없다.**

**사용자가 설계 결함 둘을 더 잡았다** - 운동 이름을 비우면 그 운동이 지워지던 설계(다른 루틴이 참조하던 운동까지 잃는다), 날짜별 계획에 운동 0개를 허용하던 설계(계획일로는 세어지는데 시작할 길이 없다). 둘 다 고쳤다.

**독립 검사 두 갈래로 29건이 나왔고 전량 반영했다.** 기록이 깨지는 급이 둘 - 운동 중인 오늘의 계획을 지울 수 있던 것(사양 3.9에 빈틈이 있어 조항을 먼저 고쳤다), 빈 루틴을 불러오면 그 날짜 계획이 통째로 사라지던 것. 막다른 골목 하나 - 그 달에 계획이 하루라도 있으면 월간 생성이 영구히 막히던 것(사용자 결정을 구현이 과하게 적용했다. 결정이 막으려던 것은 덮어쓰기이고 그것은 이미 있는 날짜 건너뛰기로 지켜진다).

**월간 계획 생성이 지난 날짜를 만들던 것은 확정·반영했다(2026-09-18).** 9월 18일에 이번 달 계획을 만들었더니 1일부터 17일까지 열한 날이 미실시로 찍히고 달성률이 0퍼센트로 시작하던 자리다. 사용자 결정으로 이번 달은 오늘부터 월말까지(오늘 포함), 다음 달 이후는 그 달 전체, 지난 달은 생성 조작 자체를 내지 않는다. 기획서 9.5 · 사양 3.7.1 · 설계 4.7.6.1에 적고 `planner.js`까지 고쳤다.

**다음 행동은 사용자 확인이다.** 실기기에서 한 바퀴를 직접 밟아 보는 자리다. 그 뒤에 볼 것 - `manage.js`가 1000줄을 넘어 화면 묶음별로 가르는 일(설계 2.2.1에 적어 두었다).

## 직전 작업 (2026-09-17, 오늘운동 TodayFit - 저장·브라우저 층 + 화면 셋)

**운동 실행 화면이 섰고 아이폰 세로 크기(390x844)로 전 구간을 실제로 밟아 확인했다.** 커밋 다섯(`a87d41c` → `b3c9f77` → `cdc047e` → `6218a65` → `bef7c4e`), 미커밋 0, origin 반영 완료. 테스트 151건 통과.

**이번 판의 범위는 저장·브라우저 층과 화면 셋이다** - 오늘 `S-01` · 운동 실행 `S-12` · 종료 요약 `S-13`. 사용자가 "관리 화면을 전부 완성한 뒤 확인하지 말고 운동 실행 화면이 나오면 먼저 보여 달라"고 지시해 그 셋까지만 했다.

**운동 실행 화면은 다른 화면과 기준이 다르다.** 손에 들고 보는 화면이 아니라 폰을 세워 두고 떨어져 곁눈으로 본다. 운동명(39px)·세트·큰 시계(101px)가 화면 대부분을 차지하고 나머지를 가장자리로 밀었다. `다음` 버튼은 세트마다 누르는 유일한 조작이라 폭 전체에 높이 124px 로 다른 버튼의 두 배가 넘는다. 확인용 스크린샷 13장이 `apps/todayfit/_shots/보기.html` 에 있다(git 추적 밖, 사용자가 본 뒤 지운다).

**독립 검사를 다섯 바퀴 돌렸다** - 1회차 19건 → 2회차 9건 → 3회차 4건 → 4회차 2건 → 5회차 0건. 5회차가 "사용자 확인 단계로 넘겨도 된다"로 판정했다. 기록이 깨지는 급은 셋이었다 - 전환 구간에서 길게 누르면 다 마친 앞 운동이 건너뜀으로 강등되던 것, 살아 있던 시점 표식이 구간이 바뀌어도 안 비워져 쉬지도 않은 휴식이 깎이던 것, 실행 화면 뒤로 가기 차단이 아예 없던 것.

**네 바퀴 내내 걸린 것은 기능이 아니라 상태 서술이었다.** "19건 전량 반영"이 실제로는 다섯 미해결이었고, 못 고치고 남긴 것의 범위를 두 번 연속 실제보다 작게 적었다. 다음 판에서는 고친 자리가 아니라 지적이 세운 조건을 기준으로 센다.

**사용자 결정이 하나 대기 중이다** - 전환 구간에서 길게 누를 때 무엇을 건너뛸지. 사양 4.2 표가 전환 구간을 길게 누름 대상에 넣어 두었으나 그 자리의 '현재 운동'은 이미 끝난 운동이라, 그대로 두면 기록이 깨진다. 지금은 막아 두었고 `01_spec.md` 4.2.1 에 결정 대기로 적었다.

**허브에 등재하고 배포했다(v239).** 사용자 지시로 GitHub Pages 에서 열리는 상태가 됐고, 허브 앱 탭 → 카드 → 앱 진입까지 실제로 밟아 확인했다. **다만 지금 들어가면 `오늘 예정된 운동 없음` 화면 하나만 보인다** - 계획을 만들 화면이 없어서다.

**다음 행동은 달력과 관리 화면 여덟이다**(`S-02` · `S-04`~`S-11`). **이 화면들이 없어 앱 안에서 운동 템플릿·루틴·계획을 만들 길이 아직 없다** - 지금 상태로는 저장소에 값을 직접 심어야 운동 시작 버튼이 뜬다. 허브 등록부 등재도 그때 함께 한다(지금 등재하면 카드가 들어가도 아무것도 못 하는 입구가 된다).
