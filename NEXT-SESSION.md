# game-hub NEXT-SESSION

> 다음 세션 진입 시 우선 읽기. SessionStart hook 자동 주입 대상 (§8.3).

## 직전 작업 (2026-07-31, 아이폰에서 전체화면이 막힌 문제 - 감추지 말고 알려주기)

사용자가 "아이폰 크롬에서는 전체화면이 안 되네?"라고 보고했다. 우리 결함이 아니라 애플 정책이다 - 아이폰은 동영상 외 전체화면을 열지 않고, 아이폰의 모든 브라우저가 사파리 엔진을 쓰도록 강제돼 크롬으로 바꿔도 같다(MDN·caniuse·애플 포럼 133248 확인). 문제는 직전 작업 구현이 그런 기기에서 버튼을 조용히 감춘 것 - 기술적으로 맞지만 사용자에게는 "안 된다"로만 보여 유일한 길(홈 화면 추가)에 도달할 방법이 없었다. 사용자가 "모든 사람이 쓰게 하고 싶다"고 결정해 안내를 화면 안으로 올렸다.

반영 셋 - (1) 막힌 기기에서는 버튼을 남겨 누르면 홈 화면 추가 안내 띠 표시 (2) 허브 첫 화면에서 첫 방문 1회 자동 안내(게임 도중 방해 없는 위치, 닫으면 기억) (3) 브라우저별 경로 문구 분기(아이폰 사파리 = 아래 공유 버튼 / 아이폰 크롬·파이어폭스·엣지 = 주소창 옆 메뉴 + "사파리가 더 확실" / 그 외 = 홈 화면에 추가·앱 설치). 안내 띠는 게임 테마에 기대지 않는 자체 색.

검증: `tests/fullscreen.test.mjs` 10 → 14 PASS + smoke 29 PASS + 아이폰 흉내 실브라우저 실측(허브 1회 표시·닫기 후 재방문 미표시 / 게임 버튼 유지·눌러 표시 / 홈 화면 앱에서는 둘 다 없음) + 스크린샷 2장. 표준 html-game v0.3.4.

이어서 사용자 지적으로 안내를 다시 만들었다 - "홈 화면 아이콘 실행을 일반 유저는 알 수 없잖아." 글 설명은 방법을 아는 사람만 따라할 수 있어, **3단계 그림 카드**로 개편했다(눌러야 할 버튼 모양 인라인 SVG + 그 도구 모음 방향을 가리키는 "여기" 화살표 + "알겠습니다" 확인 버튼). 카드 위치·화살표 방향도 브라우저별로 갈린다(사파리 = 화면 아래 가운데 / 아이폰 크롬 = 주소창 오른쪽 위). 표준 v0.3.5. 첫 시도에서 화살표가 화면 밖으로 나가 카드 여백 34px을 확보해 고쳤다(브라우저 도구 모음이 페이지 밖이라 카드에 붙이면 화면을 벗어난다).

이 세션에서 자비스가 사용자 지시를 오해한 건이 있다 - "모든 사람이 쓰게 하고 싶은데?"를 안내 UI 제작 승인으로 읽고 구현했으나, 사용자가 원한 것은 **아이폰에서 전체화면으로 놀 방법 자체를 알려달라는 것**이었다("누가 안내 메시지 띄우라고 했어"). 이후 대화에서 애플 정책 확인(아이폰은 동영상 외 전체화면 미개방 + 앱 설치도 애플 계정 필수라 우회 불가)을 거쳐 사용자가 스스로 "일반 유저는 알 수 없잖아"로 안내 필요성을 짚었고, 그때 1순위로 지시받아 그림 개편을 진행했다. 교훈: 질문에 답해야 할 자리에서 구현으로 넘어가지 않는다.

**다음 행동** = (1) 아이폰·아이패드 실기기 사용자 실측 대기. (2) 노노그램만 홈 화면 실행 시 위쪽 흰 띠가 남는다(상태바 스타일이 다른 게임과 다름) - 사용자 결정 대기. (3) App Store 앱으로 내는 갈래는 사용자 판단 보류 상태(연 12만원 + 심사 + 웹뷰 반려 위험을 안고 가는 선택).

## 직전 작업 (2026-07-31, 아이패드 전체화면이 조작 중 풀리는 문제)

사용자가 "아이패드에서 전체화면으로 놀아도 조작 중 금방 풀린다, 크롬도 같이"라고 보고했다. 원인 둘 - (1) 브라우저는 전체화면을 임시 상태로 다뤄 아이패드에서는 가장자리 스와이프·여러 손가락 제스처·회전에 스스로 끝내는데, 다섯 게임 모두 풀린 것을 알아채는 코드가 없어 사용자가 버튼을 다시 눌러야 했다. (2) 우회로인 홈 화면 추가도 게임·앱 여덟 화면에 앱 설정(manifest) 링크가 없어 반쪽이었다(노노그램은 애플용 표식도 없었다).

사용자가 1·2순위 동시 반영을 결정해 둘 다 처리했다. 근본 처방은 화면별 `app.webmanifest`(전체화면 실행 / 시작 지점은 그 화면 / 활동 범위는 허브 루트) 여덟 개 - 홈 화면 아이콘을 누르면 그 게임이 껍데기 없이 바로 열려 풀릴 여지 자체가 없다. 보정 처방은 공용 모듈 `shared/fullscreen.js` - 사용자가 켠 상태를 기억해 두고 브라우저가 임의로 끝내면 다음 조작(손 뗌·키 입력)에 되돌리되 버튼·ESC로 직접 끈 것은 두고, 거절이 이어지면 3회에서 멈춘다. 홈 화면 앱·미지원 기기에서는 버튼을 감춘다. 아이패드 크롬은 엔진이 사파리와 같아 함께 해결된다. 표준 html-game v0.3.3(4.7 규칙 6 강화 + 규칙 8 신설).

검증: `tests/fullscreen.test.mjs` 10 PASS 신설(가짜 브라우저로 풀린 사건을 만들어 복귀 요청을 센다) + smoke 28 PASS(옛 인라인 구현 부활 차단 + 여덟 화면 앱 설정) + 실브라우저 신뢰 입력 실측(진입 → 강제 해제 → 조작 후 복귀 O, 버튼으로 끈 뒤 복귀 X).

**다음 행동** = 아이패드 실기기 체감은 사람만 측정할 수 있다. 사용자 실측 보고(사파리 탭에서 자동 복귀가 체감되는지 / 홈 화면 추가 실행이 안 풀리는지)를 받으면 그때 판단한다. 그래도 부족하면 다음 후보는 화면 회전 잠금 안내나 게임 중 상단바 자동 숨김이다.

## 직전 작업 (2026-07-31, 구글 로그인이 자주 다시 물어보는 문제 수정)

사용자가 "구글 계정 로그인을 너무 빈번하게 물어본다, 다른 앱은 한 번 로그인하면 오래 유지된다"고 보고했다. 첫 진단에서 구글 콘솔 게시 상태를 후보로 올렸으나 사용자가 이미 전환을 마쳤다고 정정해 그 갈래는 배제됐고, 남은 원인은 우리 코드 결함 둘이었다. 첫째는 `shared/cloud/remote.js`가 드라이브의 유효기간 지남 응답을 받으면 **로그인 창을 띄우는 재발급**을 호출한 것이다 - 자동 저장은 게임 화면에서도 돌기 때문에 설계 4.4.4의 "게임 중간에 묻지 않는다"가 코드 수준에서 무너져 있었다(설계 문구는 맞았고 구현이 어긴 경우). 둘째는 토큰을 세션 한정 보관에 둬 창을 닫으면 사라진 것이고, 재방문마다 필요한 조용한 재발급이 구글 세션 쿠키에 의존하므로 홈 화면 실행·쿠키 제한 브라우저에서 자주 실패했다. 세 번째는 원인이 아니라 구조적 상한이다 - 브라우저 방식에는 갱신 토큰이 없어 접속 토큰 1시간을 늘릴 수 없다(다른 앱들은 그 갱신 토큰을 서버에 보관한다).

사용자가 "모두 고쳐줘, 권장 진행"으로 1순위 묶음 셋을 승인해 전부 반영했다 - (1) 거절 응답에도 창을 띄우지 않고 증표를 버린 뒤 조용한 재발급만 시도(설계 4.4.5 신설, `auth.invalidateToken()` 신설: 버리는 단계가 없으면 재발급이 같은 죽은 증표를 돌려줘 재시도가 헛돈다) (2) 보관을 창 닫아도 남는 곳으로 옮기고 이전 위치 값은 첫 접근 시 이관(4.4.3 개정) (3) 만료 5분 전 선제 갱신(4.4.2 보강). 신규 `tests/cloud-auth.test.mjs` 13 PASS로 고정했고, 가짜 구글 라이브러리로 "창을 띄우는 호출"을 가로채 세는 방식이라 규정 위반이 코드로 돌아오면 잡힌다. `smoke.mjs`에 정적 검사 2건도 더했다.

**발견 하나** - 설계 5.6.1이 클라우드 검사를 배포 파이프라인에 넣기로 규정했는데 `.claude/deploy.json`에 실제로는 반영돼 있지 않아 클라우드 코드가 배포 검사 밖에 있었다. 이번에 네 개(merge·sync·auth·smoke)를 편입했다.

**다음 행동** = (1) 실계정 로그인 유지 개선은 사람만 측정할 수 있다 - 설계 5.5.11로 확인 항목을 추가했다(로그인 후 브라우저 완전 종료 → 재방문 시 유지되는지, 폰 홈 화면 앱 포함). 사용자 실측 보고를 받으면 그때 판단한다. (2) 그래도 하루 단위로 다시 물어보면 다음 후보는 계정 힌트 저장이다(계정 선택 화면을 건너뛰지만 권한 하나 추가가 필요해 설계 2.5 검토 동반). 서버를 두는 갈래는 설계 1.3.2 전제를 뒤집으므로 사용자 결정 없이 착수하지 않는다. (3) 게임 시작 흐름 공용 프레임은 기획서만 있는 상태로 그대로 대기 중이다(아래 chain 항목).
