# game-hub NEXT-SESSION

> 다음 세션 진입 시 우선 읽기. SessionStart hook 자동 주입 대상 (§8.3).

## 직전 작업 (2026-08-01, 공용 프레임 노노그램 적용 - 2단계 검증)

1단계에서 만든 공용 부품을 실제 게임에 붙여 규격의 빈틈을 찾는 단계다. 노노그램을 고른 이유는 화면이 이미 셋으로 나뉘어 있어 옮기는 폭이 가장 작았기 때문이다.

노노그램에서 바뀐 것 다섯 - 시작 화면 신설(전에는 열면 곧바로 지도), 스테이지 지도가 시작 다음 칸으로, 결과가 전용 화면에서 덮는 카드로, 되돌아가기가 화살표·기기 뒤로가기 모두 한 칸씩만, 이어서 하기 신설. 소리는 그릇을 공용으로 옮겨 게임에는 음색표만 남았다.

기존 자산 보호 둘 - 저장 키(progress·inprogress·mode·muted) 그대로라 이미 푼 기록 유지, 이 게임 레이아웃이 걸려 있던 옛 표시 클래스도 남겨 프레임이 화면을 바꿀 때 함께 맞춘다(판단은 프레임, 옛 규칙은 결과만 따라감).

**검증에서 찾은 규격 빈틈 다섯**(전부 표준 4.8 규칙 11-15로 반영, v0.4.1) - (1) 보드를 화면 가득 쓰는 게임은 플레이 화면에서 규격 상단 띠를 못 씀 → 그 화면만 띠 감추고 모서리 버튼을 공용에 연결(자리는 게임, 규칙은 공용). (2) 실패·시간 제한 없는 게임은 자동 잠깐 멈춤이 방해 → 끄는 선택지. (3) 결과 카드에 글자 아닌 것(완성 그림·별점)이 옴 → 게임이 만든 요소를 그대로 받게. (4) 넓은 화면에서 조작단 폭 묶음. (5) 강조 버튼 글자색은 게임이 정함.

작업 중 실수 하나 - 소리 아이콘 상수를 프레임 생성보다 아래 둬 초기화 중 화면이 뜨지 않았다. 브라우저 오류로 즉시 잡아 위치를 올렸다.

검증 - 되돌아가기 계단 8단계 실동작(화살표·기기 뒤로가기 결과 일치, 새로고침 후 이어서 하기 복원) 통과, 브라우저 오류 0. 노노그램 테스트 20/20 + 루트 테스트 전량 PASS. 세로·가로 캡처로 보드 크기·스크롤 없음·4코너 유지 확인. 캐시 번호 v205, 기획서 Ⅰ권 r1.6.

**다음 행동** = (1) 3단계 나머지 넷 적용(사용자 지시 대기) - 비행 슈팅·테트리스는 시작 화면이 이미 있어 이동 방법 교체 위주, 러시아워·스도쿠는 시작 화면을 새로 만들어야 해 작업량이 크다. (2) 노노그램 실기기 확인은 사용자 몫. (3) 아이폰 브라우저 창 높이 실측은 종전대로 대기. (4) 글로벌 인계 대기 1건이 buffer에 올라 있다(문서 편집을 한 줄 셸 명령으로 하다 파일이 조용히 손상된 사고 + 처방안) - 글로벌 세션에서 병합. (5) PROGRESS.md가 52절 65KB라 월 단위 분리 대상인지 다음 봉합에서 판단.

## 직전 작업 (2026-08-01, 시작 흐름 공용 부품 제작 - 1단계, 게임 적용 전)

기획서 Ⅰ권 9장 세 단계 중 1단계다. 앞서 사용자 결정 셋이 확정됐다 - 시작 화면은 다섯 게임 모두에 둠 / 인트로는 다섯 모두 두지 않음(자리는 규격에 남김) / 적용은 세 단계 순서대로.

착수 전 두 갈래로 조사했다(shared에 이미 있는 것 / 다섯 게임의 화면·소리·저장 실태). 그 결과가 설계를 바꿨다 - 소리 코드가 네 게임에 같은 이름으로 복사돼 있어 공용 그릇의 함수 이름을 그것과 똑같이 맞췄고, 저장은 shared/storage.js를 반드시 감싸게 했다(직접 만지면 클라우드 동기화 신호가 끊긴다). 게임들이 .topbar를 각자 칠하고 있어 새 부품은 gg- 접두를 썼다.

만든 것은 `shared/frame/` 열 개 - text(고정 문구) / stack(되돌아가기 계단) / screens(화면 골격) / topbar / titlescreen(A안 시작 화면) / cards(잠깐 멈춤·결과) / audio / save / frame.css / index. 화면 전환은 규격대로 숨김 토글이 아니라 뿌리 요소에 적힌 화면 이름으로 가른다. 되돌아가기 계단과 조작단 아래 여백은 공용이 일괄 관리한다.

검증 - tests/frame.test.mjs 23건 신설 전량 통과 + 기존 테스트 전량 통과(smoke 29 / 전체화면 16 / 클라우드 39·29·13). 부품만으로 세 유형 시작 화면을 띄우는 확인 화면(shared/frame/demo.html)을 실브라우저로 다섯 장 캡처, 콘솔 오류 0. 문구가 다시 갈라지지 않도록 테스트가 기획서 5.3 표를 직접 읽어 코드와 대조한다.

규격은 html-game 표준 4.8로 편입(v0.4.0). 서비스 워커 미리 담기에 새 파일 10개 등재 + 캐시 번호 v204. 기획서 Ⅰ권 r1.5.

**다음 행동** = (1) 2단계 - 노노그램 하나에 부품을 붙여 규격의 빈틈을 찾는다(사용자 지시 대기). (2) 지금 부품은 게임 어디에도 연결돼 있지 않아 사용자 화면 동작은 이전과 같다. (3) 아이폰 브라우저 창이 먹는 높이 실측은 종전대로 대기.

## 직전 작업 (2026-08-01, 기획서 Ⅰ권 6장 게임별 시작 화면 그림 다섯 장 재작도)

Ⅰ권 6장의 게임별 시안 다섯 장은 시작 화면 규격을 A안으로 확정하기 전에 그린 흰 종이 도면이라 환경설정 자리와 갈래 표현이 확정 규격과 어긋나 있었다. 문서에도 그림 갱신 대기 안내가 붙어 있던 자리다. 그 다섯 장을 확정 규격 기준 실제 화면 목업으로 다시 그렸다.

목업 스타일은 Ⅱ권이 인라인으로 들고 있던 것을 `standards/html-game/plans/assets/screen-mockup.css` 공용 파일로 뽑아 두 문서가 함께 쓰게 했다. Ⅱ권은 스타일 블록을 링크로 바꾼 것 외 내용 변경 0이고 회귀 확인으로 A안 네 화면 정상.

다섯 장 모두 각 게임의 실제 자산·실제 값으로 그렸다 - 비행 슈팅은 나라별 배경 그림(파리)과 캐릭터, 테트리스는 실제 조각 일곱 종과 열 칸 판, 스도쿠는 게임에 든 입문 문제의 9칸 판, 노노그램은 완성한 그림 격자, 러시아워는 실제 48번 문제 배치와 크림 보드·출구 길. 배경 그림을 가진 게임은 비행 슈팅뿐이라 나머지 넷은 게임 자체 요소로 배경을 대신했다.

새로 확인한 것 셋 - (1) 스도쿠는 유일한 흰 바탕 게임이라 목업도 밝은 톤으로 뒤집었고, 색을 바꿔도 같은 칸 순서가 성립함을 6.7에 항목으로 추가. (2) 러시아워 주인공 그림(target.png)은 투명 배경이 아니라 어두운 사각이 따라붙어 시작 화면용 불가 - 다른 포니 그림(a_v2.png)으로 교체. (3) 옛 그림의 러시아워 모드 이름 보드게임·고난도는 실제 게임에 없는 이름이라 오리지널 186 / Fogleman 400으로 정정.

검증은 browser-shot 여덟 번, 두 문서 콘솔 오류 0. Ⅰ권 개정 r1.2.

이어서 사용자 확인 두 건 - 다섯 장 모두 손볼 곳 없음. 그리고 **시작 화면을 다섯 게임 모두에 공통 적용**하기로 확정(2026-08-01). 9장 hold와 3.2절에 확정으로 기재하고 Ⅰ권 r1.3으로 올렸다. 이어 남은 결정 둘도 확정 - 인트로는 다섯 게임 모두 두지 않음(자리는 규격에 남기고 무거운 게임이 나오면 재판단), 적용은 9장 세 단계 순서대로(공용 부품 → 노노그램 검증 → 나머지 넷). 규격에 걸린 결정이 0건이 되어 1단계 공용 부품 제작에 바로 들어갈 수 있는 상태다. Ⅰ권 r1.4.

**다음 행동** = (1) 아이폰 브라우저 창이 먹는 높이 100~150픽셀 추정값을 실기기 실측으로 확정. (2) 1단계 공용 부품 제작(다섯 화면 골격·상단 띠·결과 카드·소리·진행 저장) 착수 - 사용자 지시 대기. (3) 노노그램만 홈 화면 실행 시 위쪽 흰 띠가 남는 건은 종전대로 사용자 결정 대기.

